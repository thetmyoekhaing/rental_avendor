import 'package:flutter/material.dart';

class UserRent extends StatelessWidget {
  const UserRent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("UserRent");
  }
}
